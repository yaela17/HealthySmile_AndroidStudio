<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".gui.iniciarSesion.sign_up.Fragment_sign_up_foto_perfil">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center_horizontal">

        <!-- Fragmento con selector de imagen -->
        <FrameLayout
            android:id="@+id/fragmentProfileImageSelectorLogIng"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="32dp" />

        <!-- Botón Guardar -->
        <Button
            android:id="@+id/fragmentProfileImageSelectorLogIng_btnGuardar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Guardar"
            android:layout_marginBottom="12dp" />

        <!-- Botón Ir sin guardar -->
        <Button
            android:id="@+id/fragmentProfileImageSelectorLogIng_btnIrSinGuardar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ir sin guardar" />
    </LinearLayout>
</FrameLayout>
